name: notes_de_frais
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  camera: ^0.11.0+1
  permission_handler: ^11.3.1
  google_mlkit_text_recognition: ^0.11.0
  # Mise à jour vers des versions modernes pour résoudre les conflits
  file_picker: ^6.2.0
  native_pdf_renderer: ^3.1.0
  path_provider: ^2.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true